[paths]
input_dir = /mnt/ss5acq/SmartSPIM_Data_test
output_dir = /home/smartanalytics/SmartSPIM_Data_live
progress_log = /home/smartanalytics/SmartSPIM_Data_live/progress_log.txt

[params]
#Number of workers for batch loading, writing, and processing in destriping (Default: # CPU cores)
workers  = 24

# Use GPU (1) or not (0)
use_gpu = 1

# ram loadsize. Make 0 if want it to infer
ram_loadsize = 500

# GPU chunk size (if null, then infers from GPU memory)
# Use 32 for GPU's with 16GB or 12GB VRAM, 64 for GPUs with 24GB
# For GPUs with 48GB vRMAM, use 128
gpu_chunksize = 64


#Chunk size for batch processing (Default: 1)
chunks  = 1 

reconnect = True

check_corrupt = True

stall_limit = 60

time_stamp = True

version = 5
